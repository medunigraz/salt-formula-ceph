{%- set keyring = salt['pillar.get']('ceph:mon:keyring:mon') %}

[mon.]
    key = {{ keyring.key  }}
    {%- for cap_name, cap in  keyring.caps.items() %}
    caps {{ cap_name }} = "{{ cap }}"
    {%- endfor %}
